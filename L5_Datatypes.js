"use strict"; // treat all JS code as newer version
// but nowadays, it is not required to mention it, js by default treat it as strict, but still it is a good practice to write the above code

// alert( 3 + 3) // we are using nodejs, not browser

console.log(3 
    +
     3) // code readability should be high -> shouldn't be written in this way

console.log("Souvik")


let name = "souvik"
let age = 18
let isLoggedIn = false
let state;

// number => 2 to power 53
// bigint
// string => ""
// boolean => true/false
// null => standalone value   (representation of empty value)
// undefined => 
// symbol => unique


// object

console.log(typeof undefined); // o/p ->  undefined
console.log(typeof null); // o/p ->  object